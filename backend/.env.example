NODE_ENV=development
PORT=3001

# Database (Postgres). Set DATABASE_URL instead to use a full connection
# string (takes priority over the DB_* vars below).
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=postgres
DB_SSL=false

AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=trabalharn
